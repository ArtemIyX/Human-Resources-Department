<Window x:Class="Human_Resources_Department.EmployeeDebugWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Human_Resources_Department"
        mc:Ignorable="d"
        Title="EmployeeDebugWindow" Height="450" Width="800"
        Closed="Window_Closed"
        Loaded="Window_Loaded">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="244*"/>
            <RowDefinition Height="175*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="245*"/>
            <ColumnDefinition Width="151*"/>
        </Grid.ColumnDefinitions>
        <DataGrid x:Name="debug_grid" ></DataGrid>
        <DataGrid x:Name="educations_grid" Grid.Column="1"></DataGrid>
        <DataGrid x:Name="family_grid" Grid.Column="1" Grid.Row="1"></DataGrid>
    </Grid>
</Window>
